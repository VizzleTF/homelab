{{- $hc := index .Values "homelab-common" | default dict }}
{{- $jobs := $hc.backupCronJobs | default list }}
{{- $backup := (.Values.global).backup | default dict }}
{{- $nfs := $backup | dig "nfs" dict }}
{{- $webdav := $backup | dig "webdav" dict }}
{{- $rcloneImage := $backup | dig "rcloneImage" "rclone/rclone:1.69" }}
{{- $rcloneDefaultArgs := $backup | dig "rcloneDefaultArgs" (list "--verbose") }}
{{- $defaultSecCtx := dict "allowPrivilegeEscalation" false "readOnlyRootFilesystem" true "capabilities" (dict "drop" (list "ALL")) }}
{{- range $jobs }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ .name }}
  namespace: {{ $.Release.Namespace }}
spec:
  schedule: {{ .schedule | quote }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: {{ .successfulJobsHistoryLimit | default 3 }}
  failedJobsHistoryLimit: {{ .failedJobsHistoryLimit | default 3 }}
  jobTemplate:
    spec:
      backoffLimit: {{ .backoffLimit | default 2 }}
      template:
        spec:
          restartPolicy: OnFailure
          {{- if eq .type "rsync" }}
          containers:
            - name: sync
              image: instrumentisto/rsync-ssh
              {{- if .command }}
              command:
{{- toYaml .command | nindent 16 }}
              {{- else }}
              command: ["rsync", "-avz", "--no-owner", "--no-group", "--no-times", "--delete", "/source/", "/destination/"]
              {{- end }}
              securityContext:
                {{- toYaml (.securityContext | default $defaultSecCtx) | nindent 16 }}
              volumeMounts:
                - name: source-volume
                  mountPath: /source
                  {{- if .source.subPath }}
                  subPath: {{ .source.subPath }}
                  {{- end }}
                - name: dest-volume
                  mountPath: /destination
              resources:
{{- if .resources }}
{{- toYaml .resources | nindent 16 }}
{{- else }}
                requests:
                  cpu: 100m
                  memory: 128Mi
                limits:
                  cpu: "1"
                  memory: 512Mi
{{- end }}
          volumes:
            - name: source-volume
              persistentVolumeClaim:
                claimName: {{ .source.pvc }}
            - name: dest-volume
              nfs:
                server: {{ $nfs.server }}
                path: {{ printf "%s%s" $nfs.basePath .destination.path }}
          {{- else if eq .type "rclone" }}
          {{- $remoteName := .destination.remote | splitList ":" | first | upper }}
          containers:
            - name: rclone
              image: {{ $rcloneImage }}
              securityContext:
                {{- toYaml (.securityContext | default $defaultSecCtx) | nindent 16 }}
              args:
                - sync
                - /source
                - {{ .destination.remote }}
                {{- range (.rcloneArgs | default $rcloneDefaultArgs) }}
                - {{ . | quote }}
                {{- end }}
              env:
                - name: RCLONE_CONFIG_{{ $remoteName }}_TYPE
                  value: "webdav"
                - name: RCLONE_CONFIG_{{ $remoteName }}_URL
                  value: {{ $webdav.url | quote }}
                - name: RCLONE_CONFIG_{{ $remoteName }}_USER
                  value: {{ $webdav.user | quote }}
                - name: RCLONE_CONFIG_{{ $remoteName }}_VENDOR
                  value: {{ $webdav.vendor | quote }}
                - name: RCLONE_CONFIG_{{ $remoteName }}_PASS
                  valueFrom:
                    secretKeyRef:
                      name: {{ .destination.secretName }}
                      key: RCLONE_CONFIG_{{ $remoteName }}_PASS
              volumeMounts:
                - name: source-volume
                  mountPath: /source
                  readOnly: true
                  {{- if .source.subPath }}
                  subPath: {{ .source.subPath }}
                  {{- end }}
              resources:
{{- if .resources }}
{{- toYaml .resources | nindent 16 }}
{{- else }}
                requests:
                  cpu: 100m
                  memory: 256Mi
                limits:
                  cpu: "1"
                  memory: 512Mi
{{- end }}
          volumes:
            - name: source-volume
              persistentVolumeClaim:
                claimName: {{ .source.pvc }}
          {{- end }}
{{- end }}
