apiVersion: v1
kind: Service
metadata:
  name: {{ include "tracktor.fullname" . }}
  labels:
    {{- include "tracktor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tracktor.selectorLabels" . | nindent 4 }}
