# Cilium CNI values
# Non-default values used in the cluster

# Cilium-agent resources (DaemonSet, one pod per node — currently 3)
# Working set sat at 470-490Mi of 512Mi (7d, 2026-09): raised before the
# first OOM kills the datapath agent.
resources:
  requests:
    memory: 384Mi
    cpu: 96m
  limits:
    memory: 768Mi
    cpu: 400m

# GOMEMLIMIT — chart auto-injects via downward API from limits.memory; do NOT
# add via extraEnv (causes "duplicate entries for key" SSA failure).

envoy:
  resources:
    requests:
      memory: 64Mi
      cpu: 10m
    limits:
      memory: 256Mi
      cpu: 200m

operator:
  resources:
    requests:
      memory: 64Mi
      cpu: 25m
    limits:
      memory: 256Mi
      cpu: 250m
  extraEnv:
    - name: GOMEMLIMIT
      value: "220MiB"

# IP Address Management (IPAM) configuration
ipam:
  mode: kubernetes

# Security context capabilities
securityContext:
  capabilities:
    ciliumAgent:
      - CHOWN
      - KILL
      - NET_ADMIN
      - NET_RAW
      - IPC_LOCK
      - SYS_ADMIN
      - SYS_RESOURCE
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    cleanCiliumState:
      - NET_ADMIN
      - SYS_ADMIN
      - SYS_RESOURCE

# Cgroup configuration
cgroup:
  autoMount:
    enabled: false
  hostRoot: /sys/fs/cgroup

# Gateway API support
gatewayAPI:
  enabled: true
  enableAlpn: true
  enableAppProtocol: true
  # New in Cilium 1.20, defaults to true. Cilium 1.19 had no such flag and ran
  # Envoy with use_remote_address=false. Keeping it false preserves the pre-1.20
  # real-IP behaviour behind cloudflared (x-envoy-external-address would otherwise
  # become the cloudflared pod IP). Flip deliberately, not as an upgrade side effect.
  useRemoteAddress: false

# Native routing (all nodes on same L2: 10.11.11.x)
routingMode: native
autoDirectNodeRoutes: true
ipv4NativeRoutingCIDR: "10.244.0.0/16"

# WireGuard transparent encryption (pod-to-pod, cross-node)
encryption:
  enabled: true
  type: wireguard

# eBPF masquerade instead of IPTables
bpf:
  masquerade: true

# Hubble observability platform
# Agent metrics (:9962, scraped by manifests/vmpodscrape.yaml). Without them a
# node dropping ~2 packets/s to a stale iSCSI target for weeks went unnoticed
# (cilium_drop_count_total{reason="Stale or unroutable IP"}, 2026-09-24).
prometheus:
  enabled: true

hubble:
  relay:
    enabled: true
    resources:
      requests:
        memory: 32Mi
        cpu: 10m
      limits:
        memory: 128Mi
        cpu: 200m
  ui:
    enabled: false

# Kubernetes API server configuration
k8sServiceHost: localhost
k8sServicePort: 7445

# Replace kube-proxy with Cilium's eBPF implementation
kubeProxyReplacement: true

# Socket-level LB — translate service→backend at the connect()/sendmsg() cgroup
# hook, eliminating per-packet DNAT for east-west ClusterIP traffic. Was observed
# OFF in live cilium-config (bpf-lb-sock: false) despite kube-proxy replacement;
# set explicitly. Verify post-sync that bpf-lb-sock flips to true.
socketLB:
  enabled: true

# Bandwidth Manager — EDT/eBPF egress shaping + BBR congestion control. Improves
# throughput/latency for user-facing upload/download via cloudflared. Requires
# eBPF host routing (active: Masquerading=BPF) and kernel >=5.18 (Talos 6.18).
# Takes effect after pod restart.
bandwidthManager:
  enabled: true
  bbr: true

# Enable L7 proxy for Ingress Controller support
l7Proxy: true

# BGP Control Plane — eBGP peering with OpenWrt (10.11.11.1, ASN 65000).
# CRs live in manifests/cilium-bgp.yaml. MD5 auth deliberately disabled
# (OpenWrt 25.12 kernel lacks CONFIG_TCP_MD5SIG).
bgpControlPlane:
  enabled: true

# Ingress Controller — disabled, Gateway API used exclusively
ingressController:
  enabled: false
